/**
 * GrainBackground — applies a repeating grain/noise texture overlay across the page.
 * Matches the yellow + grain vibe from assets/ref/dreamhouse.png.
 */
import './GrainBackground.css';

export default function GrainBackground() {
  return (
    <div className="grain-background" aria-hidden="true" />
  );
}
